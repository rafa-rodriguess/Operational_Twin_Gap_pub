# Research questions

Study type **EMP**. Protocol in `config/protocol.py`. Numbers in `artifacts/sot.json`.

## RQ1

Operational Twin Gap (OTG): plant-balanced mean of directional zero-shot transfer gaps on exact nominal twins under CS4.

## RQ2

Directional asymmetry of OTG: plant-balanced mean absolute asymmetry on bidirectionally computable unordered twin pairs.

## RQ3

Same-state matched control minus twin contrast (`D_fleet`), pairwise CS4 intersection, F1 target-balanced mean over computable targets.
